<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.college.yi.bookmanager.repository.bookMapper">

    <!-- 書籍一覧取得 -->
<select id="findAll" resultType="com.college.yi.bookmanager.model.Book">
    SELECT id, title, author, publisher, published_date, stock
    FROM public.books
</select>


    <!-- 書籍登録 -->
    <insert id="insert" parameterType="com.college.yi.bookmanager.model.Book">
        INSERT INTO books (title, author, publisher, published_date, stock)
        VALUES (#{title}, #{author}, #{publisher}, #{publishedDate}, #{stock})
    </insert>

    <!-- 書籍更新 -->
    <update id="update" parameterType="com.college.yi.bookmanager.model.Book">
        UPDATE books
        SET
            title = #{title},
            author = #{author},
            publisher = #{publisher},
            published_date = #{publishedDate},
            stock = #{stock}
        WHERE id = #{id}
    </update>

    <!-- 書籍削除 -->
    <delete id="delete" parameterType="int">
        DELETE FROM books
        WHERE id = #{id}
    </delete>

</mapper>
